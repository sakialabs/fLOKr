[pytest]
DJANGO_SETTINGS_MODULE = flokr.settings
python_files = tests.py test_*.py *_tests.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --strict-markers
    --tb=short
testpaths = 
    users
    inventory
    reservations
    hubs
    community
    partners
    ori_ai
markers =
    unit: Unit tests
    integration: Integration tests
    property: Property-based tests
